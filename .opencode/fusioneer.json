{
  "$schema": "https://opencode.ai/fusioneer.json",
  "triggers": {
    "issues": ["labeled"],
    "pull_request": ["opened", "synchronize"],
    "issue_comment": ["created"],
    "pull_request_review_comment": ["created"]
  },
  "labels": {
    "auto": "fusioneer:auto"
  },
  "verify": "python -m unittest discover -s tests -v && python scripts/docs_check.py",
  "hardware_verify": "python scripts/fiducial.py lint && python scripts/fiducial.py erc && python scripts/fiducial.py check-intent",
  "revision": {
    "triggers": ["/fusioneer", "/revise"],
    "maxRevisions": 3
  }
}
