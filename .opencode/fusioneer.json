{
  "$schema": "https://opencode.ai/fusioneer.json",
  "triggers": {
    "issues": ["labeled"],
    "pull_request": ["opened", "synchronize"],
    "issue_comment": ["created"],
    "pull_request_review_comment": ["created"]
  },
  "labels": {
    "auto": "fusioneer:auto"
  },
  "verify": "python3 -m unittest discover -s tests -v && python3 scripts/docs_check.py",
  "hardware_verify": "python3 scripts/fiducial.py lint && python3 scripts/fiducial.py erc && python3 scripts/fiducial.py check-intent",
  "revision": {
    "triggers": ["/fusioneer", "/revise"],
    "maxRevisions": 3
  }
}
